Currently Added:
ListInterface.java
ArrayList.java
LinkedList.java

Methods:
public void add(T newEntry); //Adds a new entry to the end of the list

public void add(int newPosition, T newEntry); //Adds a new entry to a desired position

public T remove(int givenPosition); //Removes at a given position

public void clear(); //Removes everything

public T replace(int givenPosition, T newEntry); //Replaces a desired position with a new entry

public T getEntry(int givenPosition); //Gets an entry at a desired postion

public T[] toArray(); //Turns the list into an array

public boolean contains(T anEntry); //Checks if an object exists

public int getLength(); //Gets the current length of the list

public boolean isEmpty(); //Checks if the list is empty

Remember, All of my code is spaghetti code, it may be a little messy
and slow. Eventhough the code works, the professor may not like how
I coded each of the methods in the interfaces.

You are free to edit and use my source code all you like.

-Henry